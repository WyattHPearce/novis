class BuildManager {
  constructor(){
    // Coordinates within the world array of the chunk that the mouse is hovering
    this.currentChunkX;
    this.currentChunkY;

    // Coordinates of the tile within the chunk
    this.currentTileX;
    this.currentTileY;
  }

  update(){
    

    // // Loop through all chunks in the world array
    // for (let x = 0; x < worldManager.horizontalChunks; x++) {
    //   for (let y = 0; y < worldManager.verticalChunks; y++) {
    //     let chunk = worldManager.world[x][y];

    //   }
    // }
  }
}